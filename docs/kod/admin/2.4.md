### 多部门用户的权限说明

一个用户可以同时加入多个部门，此时能看到的文件范围会叠加。以下说明不同情况下的权限表现。

#### 仅属于一个部门

用户默认只能看到**自己所在部门**的文件和文件夹。

#### 属于多个部门

用户可以看到**所有已加入部门**的文件。

此外，若管理员在「企业网盘」中开启了「罗列子部门」，并且为其他部门单独设置了包含该用户的文档权限，用户也能看到对应部门的文件。

> **提示**
>
> 多部门用户的最终权限，遵循 [2.1 权限优先原则](/admin/2.1/) 中的规则：**角色权限 > 文档权限 > 部门权限**。
