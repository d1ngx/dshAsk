### 4.2.1 数据备份

为防止系统故障、数据库损坏、服务器崩溃等意外导致数据丢失，可道云提供了数据备份功能。

备份分为两部分：

- **文件备份**：按原始路径增量备份到目标目录
- **数据库备份**：按日期完整备份到目标目录的 `database` 文件夹

可根据需要选择备份「文件 + 数据库」，或仅备份「数据库」。

### 4.2.2 数据恢复

数据恢复涉及系统环境和数据结构的复杂性，操作不当可能导致数据无法正常使用。如需从备份恢复数据，建议联系可道云官方获取专业技术支持（付费服务），以确保恢复过程安全可靠。
